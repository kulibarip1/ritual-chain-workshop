# Test Plan – DisputableAIBounty

- Happy path (commit → reveal → judge → finalize)
- Dispute path (initiate within window → resolve after window)
- Revert cases: reveal before deadline, dispute after window, non‑owner actions
